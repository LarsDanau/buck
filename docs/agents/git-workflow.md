# Git Workflow And Change Scope

- Commit title format: `<type>(<scope>): <title>`.
- Run `bun run check` before committing.
- Keep changes minimal.
- Do not rename packages or move folders unless the task requires it.
- Avoid broad refactors during feature work.
